import Calender from "../comp/Comp_Calender";



function Tim() {
    return ( 
        <>
         
          <Calender/>
        </>

         



// {names.map((writeData) => (
//   <p>{writeData.name}</p>
// ))}


// filter(person => person.name === "Alice").


// {names.filter(person => person.name === "Alice").map((writeData) => (
//   <p>{writeData.name}</p>
// ))}

     );
}

export default Tim;